import { useState } from 'react'
import './TrackWorkout.css'

export default function TrackWorkoutPage({workoutId}) {
    let {workoutId} = useParams()
    const [workout, setWorkout] = useState()
    const [index, setIndex] = useState()
    
    return (

    )
}